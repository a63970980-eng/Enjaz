export function workforceInsights(employees=[],tasks=[]){const active=employees.filter(e=>!['paused','disabled','inactive'].includes(String(e.status||'').toLowerCase())).length;const done=tasks.filter(t=>['completed','done','success'].includes(String(t.status||'').toLowerCase())).length;const rate=tasks.length?Math.round(done/tasks.length*100):0;const cost=employees.reduce((s,e)=>s+Number(e.budgetCents||0),0)/100;return `<section class="workforce-insights"><div class="insight-intro"><div class="eyebrow">WORKFORCE INTELLIGENCE</div><h2>هل القوة الرقمية تعمل بكفاءة؟</h2><p>نظرة تشغيلية سريعة على الاستقلالية، الحمل، والنتائج.</p></div><div class="insight-metrics"><div><span>قوة نشطة</span><b>${active}</b><small>موظفون جاهزون للعمل</small></div><div><span>معدل إتمام</span><b>${rate}%</b><small>${done} نتيجة مكتملة</small></div><div><span>ميزانية شهرية</span><b>$${cost.toLocaleString('en-US',{maximumFractionDigits:0})}</b><small>حسب الإعدادات الحالية</small></div></div></section>`}