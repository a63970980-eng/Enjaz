const navItems=[['dashboard','الرئيسية','⌂'],['employees','الموظفون الرقميون','◉'],['tasks','المهام','✓'],['teams','الفرق الذكية','◌'],['workflows','سير العمل','↯'],['approvals','الموافقات','◇'],['integrations','التكاملات','⊕'],['audit','سجل التدقيق','▤']];
const escapeHtml=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
function go(section){const button=document.querySelector(`[data-nav="${section}"]`);if(button)button.click()}
function installObjectNav(){const main=document.querySelector('.enjaz-main');const top=document.querySelector('.enjaz-topbar');if(!main||!top||document.querySelector('.enjaz-object-nav'))return;const nav=document.createElement('nav');nav.className='enjaz-object-nav';nav.setAttribute('aria-label','التنقل الرئيسي');nav.innerHTML=`<div class="enjaz-object-brand"><span class="launcher-grid">⠿</span><span>إنجاز</span><small>Digital Workforce</small></div><div class="enjaz-object-links">${navItems.map(([id,label,icon])=>`<button type="button" data-object-nav="${id}"><i>${icon}</i><span>${label}</span></button>`).join('')}</div><button type="button" class="nav-more" data-command-open>المزيد <span>⌄</span></button>`;top.insertAdjacentElement('afterend',nav);nav.querySelectorAll('[data-object-nav]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.objectNav)));nav.querySelector('[data-command-open]')?.addEventListener('click',openCommand)}
function syncObjectNav(){const active=document.querySelector('.enjaz-nav button.active')?.dataset.nav||'dashboard';document.querySelectorAll('[data-object-nav]').forEach(b=>b.classList.toggle('active',b.dataset.objectNav===active))}
function installPageTools(){const content=document.getElementById('content');if(!content||content.querySelector('.enjaz-page-tools'))return;const section=document.querySelector('.enjaz-nav button.active')?.dataset.nav||'dashboard';if(!['employees','tasks','teams','workflows','approvals','integrations','audit'].includes(section))return;const head=content.querySelector('.pagehead');if(!head)return;const tools=document.createElement('div');tools.className='enjaz-page-tools';tools.innerHTML=`<div class="list-view-label"><span>قائمة العمل</span><strong>${escapeHtml(head.querySelector('h1')?.textContent||'')}</strong></div><label class="list-search"><span>⌕</span><input type="search" placeholder="بحث داخل القائمة..." aria-label="بحث داخل القائمة"></label><button type="button" class="list-tool" data-density title="كثافة العرض">☷</button><button type="button" class="list-tool" data-command-open title="الأوامر">⌘</button>`;head.insertAdjacentElement('afterend',tools);tools.querySelector('input')?.addEventListener('input',e=>filterCurrentList(e.target.value));tools.querySelector('[data-density]')?.addEventListener('click',()=>document.documentElement.classList.toggle('enjaz-compact'));tools.querySelectorAll('[data-command-open]').forEach(b=>b.addEventListener('click',openCommand))}
function filterCurrentList(value){const q=value.trim().toLowerCase();document.querySelectorAll('.employee-card,.table-row,.team-card,.workflow-row,.approval-row,.integration-card,.audit-row').forEach(row=>{row.style.display=!q||(row.textContent||'').toLowerCase().includes(q)?'':'none'})}
function installCommand(){if(document.querySelector('.enjaz-command'))return;const el=document.createElement('div');el.className='enjaz-command';el.hidden=true;el.innerHTML=`<div class="command-backdrop" data-command-close></div><section class="command-panel" role="dialog" aria-modal="true" aria-label="مركز الأوامر"><div class="command-head"><div><span class="eyebrow">ENJAZ COMMAND</span><h2>اذهب إلى أي مكان</h2></div><button type="button" class="command-close" data-command-close>Esc</button></div><label class="command-input"><span>⌕</span><input autofocus placeholder="ابحث عن صفحة أو إجراء..." aria-label="بحث الأوامر"></label><div class="command-items">${navItems.map(([id,label,icon])=>`<button type="button" data-command-nav="${id}"><i>${icon}</i><span><strong>${label}</strong><small>فتح مساحة ${label}</small></span><kbd>↵</kbd></button>`).join('')}</div></section>`;document.body.appendChild(el);el.querySelectorAll('[data-command-close]').forEach(b=>b.addEventListener('click',closeCommand));el.querySelectorAll('[data-command-nav]').forEach(b=>b.addEventListener('click',()=>{go(b.dataset.commandNav);closeCommand()}));const input=el.querySelector('input');input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();el.querySelectorAll('[data-command-nav]').forEach(b=>b.hidden=q&&!b.textContent.toLowerCase().includes(q))});document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openCommand()}if(e.key==='Escape')closeCommand()})}
function openCommand(){const el=document.querySelector('.enjaz-command');if(!el)return;el.hidden=false;document.body.classList.add('command-open');setTimeout(()=>el.querySelector('input')?.focus(),20)}
function closeCommand(){const el=document.querySelector('.enjaz-command');if(!el)return;el.hidden=true;document.body.classList.remove('command-open')}
function installUtilityBar(){if(document.querySelector('.enjaz-utility-bar'))return;const bar=document.createElement('div');bar.className='enjaz-utility-bar';bar.innerHTML='<button type="button">◷ <span>آخر النشاط</span></button><button type="button">▣ <span>المهام الأخيرة</span></button><button type="button">? <span>المساعدة</span></button><span class="utility-spacer"></span><small>إنجاز · مساحة العمل الرئيسية · متصلة</small>';document.body.appendChild(bar)}
function boot(){installObjectNav();installCommand();installUtilityBar();installPageTools();syncObjectNav()}
const observer=new MutationObserver(()=>boot());observer.observe(document.body,{subtree:true,childList:true});boot();
