export function taskForm(employees=[]){return `<form id="task-form" class="employee-form"><div class="form-grid"><label>عنوان المهمة<input name="title" required maxlength="160" placeholder="مثال: تحليل مبيعات هذا الأسبوع"></label><label>الموظف الرقمي<select name="employeeId" required>${employees.length?employees.map(e=>`<option value="${String(e.id).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#39;')}">${String(e.name||'موظف AI').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#39;')}</option>`).join(''):'<option value="">لا يوجد موظفون</option>'}</select></label><label class="full">الهدف<textarea name="objective" required maxlength="4000" placeholder="ما النتيجة المطلوبة من هذه المهمة؟"></textarea></label><label>الأولوية<select name="priority"><option value="5">متوسطة</option><option value="1">عاجلة</option><option value="3">عالية</option><option value="7">منخفضة</option><option value="10">الأقل</option></select></label></div><div class="form-actions"><button type="button" class="ghost" id="cancel-task">إلغاء</button><button class="primary" type="submit" ${employees.length?'':'disabled'}>إنشاء المهمة</button></div></form>`}
export async function submitTask(form,{workspaceId,token,apiClient}){const f=new FormData(form);const title=String(f.get('title')||'').trim();const employeeId=String(f.get('employeeId')||'').trim();const objective=String(f.get('objective')||'').trim();const priority=Number(f.get('priority')||5);if(!title||!employeeId||!objective)throw new Error('أكمل عنوان المهمة والموظف والهدف');if(!Number.isInteger(priority)||priority<1||priority>10)throw new Error('الأولوية يجب أن تكون بين 1 و10');return apiClient.createTask(workspaceId,token,{title,employeeId,objective,priority});}
