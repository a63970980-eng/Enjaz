import {sectors,findRole} from './employee-catalog.js';
let selectedSector=sectors[0]?.id||'';let selectedRole=sectors[0]?.roles[0]?.[0]||'';
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const roleObject=()=>findRole(selectedRole);
export function employeeForm(){const s=sectors.find(x=>x.id===selectedSector)||sectors[0];const r=roleObject();return `<form id="employee-form" class="employee-form"><input type="hidden" name="catalogRole" value="${esc(selectedRole)}"><div class="form-intro"><div class="form-orb">✦</div><div><strong>اختر موظفًا رقميًا جاهزًا</strong><p>لا تبدأ من الصفر. اختر القطاع ثم الدور المتخصص الذي تريد تشغيله داخل مؤسستك.</p></div><div class="form-live"><span class="live-dot"></span><span>كتالوج إنجاز</span></div></div><div class="form-section"><div class="form-section-title"><b>01</b><div><strong>اختر قطاع مؤسستك</strong><small>إنجاز يخصص القوى العاملة حسب طبيعة المؤسسة.</small></div></div><div class="sector-grid">${sectors.map(x=>`<button type="button" class="sector-card ${x.id===selectedSector?'selected':''}" data-sector="${x.id}"><span class="sector-icon">${x.icon}</span><strong>${x.name}</strong><small>${x.desc}</small></button>`).join('')}</div></div><div class="form-section"><div class="form-section-title"><b>02</b><div><strong>اختر الموظف المتخصص</strong><small>${s.name} · أدوار مصممة مسبقًا للتشغيل.</small></div></div><div class="role-grid">${s.roles.map(x=>`<button type="button" class="role-card ${x[0]===selectedRole?'selected':''}" data-role="${x[0]}"><span>✦</span><div><strong>${x[1]}</strong><small>${x[2]}</small></div><b>اختيار</b></button>`).join('')}</div></div><div class="form-section"><div class="form-section-title"><b>03</b><div><strong>تخصيص التشغيل</strong><small>الهوية التشغيلية جاهزة؛ يمكنك فقط تسمية الموظف داخل مؤسستك.</small></div></div><div class="form-grid"><label>اسم العرض داخل المؤسسة<input name="name" required maxlength="120" value="${esc(r?.name||'موظف رقمي')}" placeholder="مثال: مدير المطعم — الفرع الرئيسي"></label><label>الدور<input name="role" readonly value="${esc(r?.role||'موظف رقمي')}"></label><label class="full">الهدف التشغيلي<textarea name="goal" readonly>${esc(r?.goal||'')}</textarea></label><label>الاستقلالية<select name="autonomy"><option value="guided">موجّه — يطلب التأكيد عند الحاجة</option><option value="balanced" selected>متوازن — يعمل ضمن السياسة</option><option value="autonomous">مستقل — ضمن الحدود المسموحة</option></select></label><label>الميزانية الشهرية<input name="budget" type="number" min="0" max="1000000" step="0.01" value="50"></label><label>سياسة الموافقة<select name="approval"><option value="required">للعمليات الحساسة فقط</option><option value="always">قبل كل إجراء</option><option value="none">بدون موافقة منخفضة المخاطر</option></select></label><label>جدول العمل<select name="schedule"><option value="always">متاح دائمًا</option><option value="business_hours">ساعات العمل</option><option value="scheduled">وفق جدول مخصص</option></select></label></div></div><div class="form-section summary-section"><div><strong>${esc(r?.name||'الموظف الرقمي')}</strong><p>${esc(r?.goal||'سيتم تشغيل الدور ضمن مساحة العمل.')}</p></div><span class="ready-badge">● جاهز للتشغيل</span></div><div class="form-actions"><button type="button" class="ghost" id="cancel-employee">إلغاء</button><button class="primary" type="submit">إضافة إلى فريق المؤسسة</button></div></form>`}
function rerenderForm(){const host=document.querySelector('#employee-form');if(!host)return;host.outerHTML=employeeForm();const next=document.querySelector('#employee-form');if(next)bindEmployeeForm(next)}
export function bindEmployeeForm(form){form.querySelectorAll('[data-sector]').forEach(b=>b.onclick=()=>{selectedSector=b.dataset.sector;const s=sectors.find(x=>x.id===selectedSector);selectedRole=s?.roles[0]?.[0]||'';rerenderForm()});form.querySelectorAll('[data-role]').forEach(b=>b.onclick=()=>{selectedRole=b.dataset.role;rerenderForm()})}
export async function submitEmployee(form,{workspaceId,token,apiClient}){const f=new FormData(form);const preset=findRole(String(f.get('catalogRole')||''));const name=String(f.get('name')||'').trim();const budget=Number(f.get('budget')||0);if(!preset)throw new Error('اختر موظفًا من كتالوج إنجاز');if(!name)throw new Error('أدخل اسم العرض للموظف');if(!Number.isFinite(budget)||budget<0||budget>1000000)throw new Error('الميزانية غير صالحة');return apiClient.createEmployee(workspaceId,token,{name,role:preset.role,goal:preset.goal,model:'auto',autonomy:String(f.get('autonomy')||'balanced'),skills:preset.skills,tools:preset.tools,budgetCents:Math.round(budget*100),schedule:{type:String(f.get('schedule')||'always')},policy:{approvalMode:String(f.get('approval')||'required'),autonomy:String(f.get('autonomy')||'balanced')},sector:preset.sectorId,sectorName:preset.sectorName,catalogRole:preset.id})}
