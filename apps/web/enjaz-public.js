const q=new URLSearchParams(location.search);const go=m=>{const u=new URL(location.href);u.search='';u.searchParams.set('auth','1');if(m==='signup')u.searchParams.set('signup','1');location.href=u};if(!q.has('auth')){const r=document.getElementById('enjaz-public');r?.querySelectorAll('[data-auth]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.auth)));r?.querySelectorAll('[data-scroll]').forEach(b=>b.addEventListener('click',()=>document.getElementById(b.dataset.scroll)?.scrollIntoView({behavior:'smooth'})));const s=[...(r?.querySelectorAll('.ez-step')||[])];let i=0;setInterval(()=>{if(document.hidden)return;s.forEach(x=>x.classList.remove('is-current'));s[i]?.classList.add('is-current');i=(i+1)%s.length},2600);const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('is-visible')),{threshold:.1});r?.querySelectorAll('.ez-sector-band,.ez-how,.ez-why,.ez-final').forEach(x=>io.observe(x));const h=r?.querySelector('.ez-hero-visual');if(h&&matchMedia('(pointer:fine)').matches){h.onpointermove=e=>{const a=h.getBoundingClientRect();h.style.setProperty('--mx',((e.clientX-a.left)/a.width-.5).toFixed(3));h.style.setProperty('--my',((e.clientY-a.top)/a.height-.5).toFixed(3))};h.onpointerleave=()=>{h.style.setProperty('--mx','0');h.style.setProperty('--my','0')}}}